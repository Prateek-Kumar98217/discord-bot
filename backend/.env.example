# ─── Discord Bot ──────────────────────────────────────────────────────────────

# Your Discord bot token (from https://discord.com/developers/applications)
DISCORD_TOKEN=your_discord_bot_token_here

# Application / Client ID of your Discord app
DISCORD_CLIENT_ID=your_discord_client_id_here

# Guild (server) ID used for registering slash commands during development
DISCORD_GUILD_ID=your_discord_guild_id_here

# URL of the FastAPI backend that the bot POSTs audio to
BACKEND_API_URL=http://localhost:8000/audio

# Milliseconds of silence before a voice clip is considered complete
SILENCE_DURATION_MS=1000


# ─── Groq API ─────────────────────────────────────────────────────────────────

# Primary Groq API key (https://console.groq.com/keys)
GROQ_API_KEY=your_groq_api_key_here

# Optional: comma-separated list of additional Groq API keys for key rotation.
# When multiple keys are provided they are used round-robin so you can spread
# load across accounts or avoid hitting per-key rate limits.
# Example: GROQ_API_KEYS=gsk_key1,gsk_key2,gsk_key3
GROQ_API_KEYS=

# Optional: comma-separated list of Groq Whisper model IDs to rotate through.
# If left empty the server uses the built-in default list:
#   whisper-large-v3-turbo, distil-whisper-large-v3-en, whisper-large-v3
# Available models: https://console.groq.com/docs/speech-text
# Example: GROQ_WHISPER_MODELS=whisper-large-v3-turbo,whisper-large-v3
GROQ_WHISPER_MODELS=


# ─── Cerebras Cloud API ───────────────────────────────────────────────────────

# Primary Cerebras API key (https://cloud.cerebras.ai)
CEREBRAS_API_KEY=your_cerebras_api_key_here

# Optional: comma-separated list of additional Cerebras API keys for key
# rotation.  Keys are used round-robin so load is spread across accounts
# and per-key rate limits are avoided automatically.
# Example: CEREBRAS_API_KEYS=key1,key2,key3
CEREBRAS_API_KEYS=

# LLM model to use for transcript analysis.
# Defaults to gpt-oss-120b if not set.
# Other available models: llama-3.3-70b, llama3.1-70b, llama3.1-8b
CEREBRAS_MODEL=gpt-oss-120b
